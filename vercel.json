{
    "version": 2,
    "buildCommand": "npm run admin:build",
    "functions": {
        "api/index.js": {
            "includeFiles": "{admin-dist,src,public}/**"
        }
    },
    "routes": [
        {
            "src": "/(.*)",
            "dest": "api/index.js"
        }
    ]
}
