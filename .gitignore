node_modules/
dist/
admin-dist/
.env
.env.*
!.env.example

# data/ IS committed — apps.json and the uploaded icons together are the
# deployable config, and the static build reads both. Ignoring the icons would
# mean a fresh clone deploys placeholder letters instead of app branding.

*.log
.DS_Store

# Generated by `npm run build:static` — do not edit by hand, the build overwrites it.
firebase.json
.firebase/
